DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cs_learning_tool
SESSION_SECRET=
CSRF_SECRET=
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Resend (https://resend.com) — used to send password-reset emails.
# Get an API key from the Resend dashboard. EMAIL_FROM must use a domain
# verified in Resend, or their shared sandbox sender for testing:
# EMAIL_FROM=EduPath <onboarding@resend.dev>
RESEND_API_KEY=
EMAIL_FROM=
