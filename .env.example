# Base URL the frontend calls for the API, including the /api/v1 prefix.
# Local dev: leave unset — apiClient.js defaults to http://localhost:4000/api/v1.
# Production (Vercel): set to the deployed Railway backend's public URL,
# e.g. https://your-backend.up.railway.app/api/v1
VITE_API_BASE_URL=
